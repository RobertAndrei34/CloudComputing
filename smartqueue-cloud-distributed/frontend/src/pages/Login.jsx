import{useState}from"react";import{useNavigate}from"react-router-dom";import{api,setAuthToken}from"../api/client";export default function Login({onLogin}){const navigate=useNavigate();const[email,setEmail]=useState("admin@demo.com");const[password,setPassword]=useState("admin123");const[error,setError]=useState("");async function login(e){e.preventDefault();setError("");try{const res=await api.post("/auth/login",{email,password});setAuthToken(res.data.access_token);const session={userId:res.data.user_id,name:res.data.name,role:res.data.role};localStorage.setItem("session",JSON.stringify(session));onLogin(session);navigate("/")}catch(err){setError("Login failed. Check email and password.")}}return <div className="card"><h2>Login</h2><p>Admin: <strong>admin@demo.com</strong> / <strong>admin123</strong></p><p>User: <strong>user@demo.com</strong> / <strong>user123</strong></p><form onSubmit={login}><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="error">{error}</p>}<button type="submit">Login</button></form></div>}
