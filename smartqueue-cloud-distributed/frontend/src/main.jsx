import React,{useEffect,useState}from"react";import ReactDOM from"react-dom/client";import{BrowserRouter,Routes,Route,Link,useNavigate}from"react-router-dom";import"./styles.css";import Home from"./pages/Home";import Login from"./pages/Login";import Appointments from"./pages/Appointments";import Queue from"./pages/Queue";import Admin from"./pages/Admin";import Notifications from"./pages/Notifications";import Analytics from"./pages/Analytics";import System from"./pages/System";import{setAuthToken}from"./api/client";
function Layout(){const navigate=useNavigate();const[session,setSession]=useState(null);useEffect(()=>{const saved=localStorage.getItem("session");if(saved)setSession(JSON.parse(saved))},[]);function logout(){setAuthToken(null);localStorage.removeItem("session");setSession(null);navigate("/login")}return <><nav className="nav"><Link to="/">SmartQueue Cloud</Link><Link to="/appointments">Appointments</Link><Link to="/queue">Queue</Link><Link to="/notifications">Notifications</Link><Link to="/admin">Admin</Link><Link to="/analytics">Analytics</Link><Link to="/system">System</Link><Link to="/login">Login</Link>{session&&<button className="nav-button" onClick={logout}>Logout</button>}</nav><main className="container">{session&&<div className="session">Logged in as <strong>{session.name}</strong> ({session.role})</div>}<Routes><Route path="/" element={<Home/>}/><Route path="/login" element={<Login onLogin={setSession}/>}/><Route path="/appointments" element={<Appointments/>}/><Route path="/queue" element={<Queue/>}/><Route path="/notifications" element={<Notifications/>}/><Route path="/admin" element={<Admin/>}/><Route path="/analytics" element={<Analytics/>}/><Route path="/system" element={<System/>}/></Routes></main></>}
function App(){return <BrowserRouter><Layout/></BrowserRouter>}ReactDOM.createRoot(document.getElementById("root")).render(<App/>);
